[project]
name = "customimpostergame"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.40.0",
    "fastapi>=0.138.0",
    "uvicorn>=0.30.0",
    "httpx>=0.27.0",
    "redis>=5.0.0",
    "segno>=1.6.0",
]

[tool.vercel]
entrypoint = "server:app"

[tool.vercel.fastapi.static]
cdn = false
